{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "nixpacks": {
      "pkgs": ["ffmpeg"],
      "apt": ["ffmpeg"]
    }
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:\ --worker-class eventlet app:app"
  }
}
