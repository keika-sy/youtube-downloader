{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "nixpacks": {
      "pkgs": ["ffmpeg"]
    }
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --worker-class eventlet app:app"
  }
}
